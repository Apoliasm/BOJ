# **[Gold V] 동전 2 - 2294**

[문제 링크](https://www.acmicpc.net/problem/2294)

### **성능 요약**

메모리: 13072 KB, 시간: 184 ms

### **분류**

다이나믹 프로그래밍

### **제출 일자**

2025년 11월 4일 16:31:33

### **문제 설명**

n가지 종류의 동전이 있다. 이 동전들을 적당히 사용해서, 그 가치의 합이 k원이 되도록 하고 싶다. 그러면서 동전의 개수가 최소가 되도록 하려고 한다. 각각의 동전은 몇 개라도 사용할 수 있다.

### **입력**

첫째 줄에 n, k가 주어진다. (1 ≤ n ≤ 100, 1 ≤ k ≤ 10,000) 다음 n개의 줄에는 각각의 동전의 가치가 주어진다. 동전의 가치는 100,000보다 작거나 같은 자연수이다. 가치가 같은 동전이 여러 번 주어질 수도 있다.

### **출력**

첫째 줄에 사용한 동전의 최소 개수를 출력한다. 불가능한 경우에는 -1을 출력한다.

# 분석

## 전형적인 DP

- **구했던 결과값에 더 갱신해서 구할 수 있을 때**
- 문제에 따라 dp 배열을 이차원이나 삼차원으로 전개

### 세부적인 조건, 결과를 출력해서 확인하기

```tsx
for (let current = 1; current <= k; current++) {
      for (const coin of coins) {
        if (current - coin < 1) continue;
        if (dp[current - coin] === 0) continue;
        **if (dp[current] === 0) {
          dp[current] = dp[current - coin] + 1;
        } else {
          dp[current] = Math.min(dp[current], dp[current - coin] + 1);
        }**
      }
    }
```

- 초기화를 0으로 해놓고 Math.min하면 무조건 0이 뜰 것..
  - dp를 출력해보고 이상있는지 파악하게 좋을수도
