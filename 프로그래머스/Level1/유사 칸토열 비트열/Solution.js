function solution(n, l, r) {
    var answer = 0;
    return answer;
    0 = 1 
    1 = 11011 f(0)*2 + g(0) + f(0)*2 
    2 = 11011 11011 00000 11011 11011 f(1) * 2 + g(1) + f(1)*2
    3 = f(2) * 2 + g*25 + f(2)*2
    const zeroDp = []
    const oneDp = []
    const resultDp = []
    zeroDp.push(0)
    oneDp.push(1)
    for(var i = 1 ; i <= 20 ; i++){
        var strZero = zeroDp[i-1]
        zeroDp[i] = `${strZero}${strZero}${strZero}${strZero}${strZero}`
        var strOne = oneDp[i-1]
        
    }
    var cantoStr = ""
    for(var i = 0 ; i<=n ; i++){
        
    }
    
}
